export type OTextChipsData = {
    currentText: string;
    texts: string[];
    addButton: [
        {
            icon: string,
            title: string,
            info: boolean
        }
    ]
}
