export type OTrainingPhrasesData = {
    currentPhrase: string;
    phrases: string[];
    addButton: [
        {
            icon: string,
            title: string,
            info: boolean
        }
    ]
}
